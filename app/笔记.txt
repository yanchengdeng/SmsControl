建议：



1.用户登录后应该返回一个状态值来表明用户是否已经校验过银行卡 具体app 步骤如下

                                if(校验过){
                                    直接走首页  直接监听已验证的银行卡
1. 用户登录完成后  ------》2.   }else{                                  --------》 3. 监听短信 ----》4 更改校验银行卡
                                    去银行卡页 走校验银行卡流程
                                 }



2. 银行卡列表返回值中 应该携带一个字段 如：isVertified 来标记 该卡的  验证状态（参考提交验证接口返回值）,最好银行卡列表中可以返回该银行卡类型的短信网关号 ：如 招商银行：95555  ，这样可以有效的过滤掉其他 不在监控范围的短信，缓解带宽压力

3.提交银行卡验证参数  应该是
{
"8":0,
"9":1.
"10":0
}

key 表示银行卡id ,value  0、1 表示对应的银卡卡是否需要验证，返回结果直接就告诉成功与否， 验证状态  由银行卡接口 可以获取


3.”商家点监听后，不允许更改监听银行卡，除非取消监听才允许更改银行卡“--》 针对这个功能点，
感觉做的有点多余了，我们只要每次上传的信息是商家最新更改监听的银行卡就可以了，没必要更改监听银行卡还需要取消监听才能重新去设置银行卡


4.最好银行卡列表中 可以返回 该银行卡类型的短信，这样可以有效的过滤掉其他 不在监控范围的短信，缓解带宽压力



2020-07-13
1.登录页 增加一个 域名配置 输入框形式 旁边有个 编辑按钮可以编辑
2.登录后 直接弹出 卡列表 选择 （最好加一个搜索）  选择验证后  显示在首页
3.首页 放几个按钮 ：取消监听、短信记录、
注意：
a.取消监听 需要重新验证卡列表
b.短信记录暂且不开发

q10000 111111
http://test1.yalalat.com/jttest


后续：
1. 日志 列表
2.版本升级